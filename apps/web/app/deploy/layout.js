export const metadata = {
  title: "Deploy Rollup Chain",
  description: "Deploy your chain!",
};

export default function Layout({ children }) {
  return <div className="min-h-[100vh]">{children}</div>;
}
